apiVersion: v1
kind: Service
metadata:
  name: backend-svc
  labels:
    app: backend
spec:
  ports:
    - name: backend
      protocol: "TCP"
      port: 5005
      targetPort: 5000
    - name: frontend
      protocol: "TCP"
      port: 8081
      targetPort: 8080
  selector:
    app: backend